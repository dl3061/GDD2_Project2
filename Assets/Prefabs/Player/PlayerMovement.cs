using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerMovement : MonoBehaviour
{

    private Vector3 initPosition;

    /*

    void Start()
    {
        initPosition = transform.position;

        uint clock = time;
        float timer = time.DeltaTime;
        Vector3 startPos;
        Vector3 endPos;
        uint charRoute = 0;

        Vector3 leftPosition = new Vector3(-10.0f,0.0f,0.0f);
        Vector3 centerPosition = new Vector3(0.0f, 0.0f, 0.0f);
        Vector3 rightPosition = new Vector3(10.0f,0.0f,0.0f);

    }

    // Update is called once per frame
    void Update()
    {
        float percent = 0f;
        // Vector3 currentPos = new Vector3.lerp(startPos, endPos, percent);
    }


    public void ResetEventHandler()
    {
        transform.position = initPosition;
    }

    */
}
